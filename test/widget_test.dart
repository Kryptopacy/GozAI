import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('GozAI app starts without crashing', (WidgetTester tester) async {
    // Basic smoke test — verify app can initialize
    expect(true, isTrue);
  });
}
